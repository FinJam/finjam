<%= form_for(@organization_tag) do |f| %>
  <% if @organization_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@organization_tag.errors.count, "error") %> prohibited this organization_tag from being saved:</h2>

      <ul>
      <% @organization_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :organization_id %><br>
    <%= f.number_field :organization_id %>
  </div>
  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.number_field :tag_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
