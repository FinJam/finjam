<%= form_for(@enterprise) do |f| %>
  <% if @enterprise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@enterprise.errors.count, "error") %> prohibited this enterprise from being saved:</h2>

      <ul>
      <% @enterprise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "What's the name of your company?" %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "About this company." %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :tags, "Tags (separated by commas)" %><br>
    <%= f.text_field :tag_list, class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
